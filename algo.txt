generate a Vue 3 component that fetch to this api https://minecraft-api.vercel.app/api/items, and render the image, description and the name of each item, use flexbox and render 4 items width

make a game fetching a limit of 200 pokemons, using a random-function to select a specific pokemon and render its shadow with the pokeshitimage component, then give the player a component with 4 options to choose, if correct, you should render the pokeshit original image, if not, show a message of incorrect answer 
